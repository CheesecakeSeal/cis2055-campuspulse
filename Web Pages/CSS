.pulse-hero {
    background-color: #f8f9fa;
    border-radius: 0.5rem;
    padding: 3rem;
    text-align: center;
    border: 1px solid #dee2e6;
    margin-bottom: 2rem;
}

.pulse-card {
    border: none;
    box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
    margin-bottom: 1rem;
    transition: transform 0.2s ease-in-out;
}

.pulse-card:hover {
    transform: translateY(-3px);
}

.status-badge {
    font-weight: 600;
    padding: 0.5em 0.75em;
    border-radius: 10px;
}

.status-open { background-color: #dc3545; color: white; }
.status-investigating { background-color: #ffc107; color: #ffffff; }
.status-resolved { background-color: #198754; color: white; }
.status-no-action { background-color: #6c757d; color: white; }

.upvote-count {
    color: #0d6efd;
    font-weight: bold;
    font-size: 0.9rem;
}